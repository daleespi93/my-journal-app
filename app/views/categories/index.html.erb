<% @categories.each do |category| %>
<tr>
  <td> <%= category.id %> </td>
  <td> <%= category.title %> </td>
  <td> <%= link_to 'Show', category_path(category.id) %> </td>
</tr>
<% end %>
<th><%= link_to 'New Category', new_category_path %> </th>