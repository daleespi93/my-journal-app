<div class="flex flex-col gap-2 w-screen h-[80vh] justify-center items-center">
  <%= form_with scope: :category, url: categories_path, method: :post, local: true do |f| %>
    <%= render 'shared/form', f: f%>
  <% end %>
</div>